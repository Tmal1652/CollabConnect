# CollabConnect (static)

## Dev tasks
- Lint CSS
  - npm run lint:css
- Build minified CSS
  - npm run build:css

Outputs `style.min.css` for production. Reference it in HTML if desired.

## Notes
- Design tokens are defined in `:root` of `style.css`.
- Mobile nav toggle and section reveal logic are in `app.js`.
